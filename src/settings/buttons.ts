import type { StarterSettings } from "./types";

const buttons: StarterSettings['buttons'] = [
  "primary",
  "secondary",
  "black",
  "gray",
  "info",
  "success",
  "warning",
  "error",
];

export default buttons;
