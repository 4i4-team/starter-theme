import styled from "styled-components";
import { Wrap as BaseWrap } from "../container.styles";

export const Wrap = styled(BaseWrap)`
  width: 100% !important;
  max-width: initial !important;
  overflow-x: hidden;
`;
