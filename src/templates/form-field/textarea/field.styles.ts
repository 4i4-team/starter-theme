import styled from "styled-components";

export const Input = styled.textarea`
  border: none;
  background-color: transparent;
  outline: none;
  line-height: inherit;
  font-size: inherit;
  font-weight: inherit;
  padding: 0;
  font-family: inherit;
  width: 100%;
`;
